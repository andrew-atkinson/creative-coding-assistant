// Refusal string is shared between server (prompt) and client (styling).
// Kept out of $lib/server so the browser bundle can import it.
export const REFUSAL =
  'I cannot find this information in the course videos. Try rephrasing, or check the syllabus for related material.';
